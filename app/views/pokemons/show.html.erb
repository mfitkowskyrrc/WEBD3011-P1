<p style="color: green"><%= notice %></p>

<%= render @pokemon %>

<div class="btn-group">
  <a href=" <%= pokemons_path %>" class="btn btn-primary" >Back to Pokemon</a>
  <a href="<%= edit_pokemon_path(@pokemon) %>" class="btn btn-primary">Edit this Pokemon</a>
  <%= button_to 'Destroy this pokemon', @pokemon, method: :delete, class: 'btn btn-danger' %>
</div>